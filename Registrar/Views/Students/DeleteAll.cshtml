@{
  Layout = "_Layout";
}
@model List<Registrar.Models.Course>;

<div class="container card">
    <h2>DELETE ALL STUDENTS?</h2>
    <hr>
    @using (Html.BeginForm())
    {
        <button class="btn btn-primary" type="submit" value="DeleteAll">YES</button>
    }
    <br>
    <strong><p>@Html.ActionLink("Back to course list", "Index")</p></strong>
</div>